name: weather_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  lottie: ^2.7.0
  http: ^0.13.3
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  connectivity: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/morning.gif
    - assets/day.gif
    - assets/evening.gif
    - assets/night.gif